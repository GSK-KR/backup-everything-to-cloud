# Optional: Override backup file location
# BACKUP_FILE=.backup

# Optional: Override config file location
# CONFIG_FILE=.config

# Optional: Local backup directory (for failed uploads)
# LOCAL_BACKUP_DIR=./backups
